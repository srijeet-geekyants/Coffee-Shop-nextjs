# ==============================================================================
# ENVIRONMENT CONFIGURATION
# ==============================================================================
# Copy this file to .env and fill in your actual values
# For development, you can use the provided defaults where applicable

# ==============================================================================
# SERVER ENVIRONMENT
# ==============================================================================

# Application Environment
NODE_ENV=development
ANALYZE=false

# ==============================================================================
# CLIENT ENVIRONMENT (Public - exposed to browser)
# ==============================================================================

# App Configuration
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_APP_TITLE="Create Next CoE"
NEXT_PUBLIC_APP_NAME="Create Next CoE"
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_DESCRIPTION="Production-ready Next.js starter"
NEXT_PUBLIC_APP_CATEGORY="app"
NEXT_PUBLIC_APP_KEYWORDS="nextjs,starter,boilerplate"

# ==============================================================================
# ANALYTICS & MONITORING (Optional for development)
# ==============================================================================

# Google Tag Manager (Optional)
# Get your GTM container ID from https://tagmanager.google.com/
NEXT_PUBLIC_GTM_KEY=GTM-XXXXXXX

# PostHog Analytics (Optional for development)
# Sign up at https://posthog.com/ for free analytics
# Personal API Key (for server-side events)
POSTHOG_API_KEY=phc_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Environment ID (found in PostHog project settings)
POSTHOG_ENV_ID=env_xxxxxxxx
# Public key (for client-side events)
NEXT_PUBLIC_POSTHOG_KEY=phc_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# PostHog host (EU: https://eu.i.posthog.com, US: https://us.i.posthog.com)
NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com
# Ingest path (proxied to avoid ad blockers)
NEXT_PUBLIC_POSTHOG_INGEST=/ingest
# Environment for PostHog
NEXT_PUBLIC_POSTHOG_ENVIRONMENT=development

# Sentry Error Tracking (Optional for development)
# Get auth token from https://sentry.io/settings/auth-tokens/
SENTRY_AUTH_TOKEN=sntrys_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================

# Database Dialect - Choose one: postgresql | mysql | sqlite
DB_DIALECT=sqlite

# SQLite (Recommended for development - no setup required)
# Uses local file database
DATABASE_URL=file:./create-next-coe.db

# PostgreSQL (via Docker or external service)
# Uncomment the lines below and comment out SQLite if using PostgreSQL
# DB_DIALECT=postgresql
# DATABASE_URL=postgresql://create-next-coe:create-next-coe@localhost:5432/create-next-coe

# MySQL (via Docker or external service)
# Uncomment the lines below and comment out SQLite if using MySQL
# DB_DIALECT=mysql
# DATABASE_URL=mysql://create-next-coe:create-next-coe@localhost:3306/create-next-coe

# ==============================================================================
# DOCKER DATABASE CONFIGURATION (Optional)
# ==============================================================================
# Only needed if using Docker for PostgreSQL/MySQL/Redis

# PostgreSQL Docker Configuration
POSTGRES_DB=create-next-coe
POSTGRES_USER=create-next-coe
POSTGRES_PASSWORD=create-next-coe
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# MySQL Docker Configuration
MYSQL_DATABASE=create-next-coe
MYSQL_ROOT_PASSWORD=create-next-coe
MYSQL_USER=create-next-coe
MYSQL_PASSWORD=create-next-coe
MYSQL_HOST=localhost
MYSQL_PORT=3306

# Redis Docker Configuration
REDIS_PASSWORD=create-next-coe
REDIS_PORT=6379

# ==============================================================================
# DEVELOPMENT NOTES
# ==============================================================================
#
# QUICK START (Minimal setup):
# 1. Copy this file to .env
# 2. Keep all defaults (SQLite database, no analytics)
# 3. Run: pnpm dev
#
# FULL SETUP (All features):
# 1. Copy this file to .env
# 2. Sign up for PostHog (free) and fill in POSTHOG_* variables
# 3. Sign up for Sentry (free) and fill in SENTRY_AUTH_TOKEN
# 4. Get GTM container ID and fill in NEXT_PUBLIC_GTM_KEY
# 5. Choose database: keep SQLite or setup PostgreSQL/MySQL
# 6. Run: pnpm dev
#
# DATABASE OPTIONS:
# - SQLite: Zero setup, perfect for development
# - PostgreSQL: Run `pnpm docker:up` for local instance
# - MySQL: Run `pnpm docker:up` for local instance
#
