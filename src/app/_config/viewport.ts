import type { Viewport } from "next";

/**
 * @description Viewport for the website
 * @returns {Viewport} Viewport for the website
 */
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  interactiveWidget: "resizes-content",
};
